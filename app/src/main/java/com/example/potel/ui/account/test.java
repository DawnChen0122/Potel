
// expanded = expandedYear,onExpandedChange = { viewModel.toggleYearDropdown() },
// modifier = Modifier.menuAnchor(MenuAnchorType.PrimaryEditable, true),value = inputYear,
//onValueChange = { viewModel.onYearChanged(it) },
//trailingIcon = { TrailingIcon(expanded = expandedYear) }expanded = expandedYear,
//  onDismissRequest = { viewModel.toggleYearDropdown() }
//   viewModel.yearRange.forEach { year -> DropdownMenuItem(text = { Text(year) },
//viewModel.onYearChanged(year)viewModel.toggleYearDropdown()

//expanded = expandedMonth,onExpandedChange = { viewModel.toggleMonthDropdown() },
//modifier = Modifier.menuAnchor(MenuAnchorType.PrimaryEditable, true),value = inputMonth.value,
//onValueChange = { viewModel.onMonthChanged(it) },
//trailingIcon = { TrailingIcon(expanded = expandedMonth) } expanded = expandedMonth,
// onDismissRequest = { viewModel.toggleMonthDropdown() }
//  viewModel.monthRange.forEach { month -> DropdownMenuItem(text ={ Text(month) },
//viewModel.onMonthChanged(month)viewModel.toggleMonthDropdown()

